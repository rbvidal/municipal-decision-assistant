package com.cognitera.platform.workspace.model;

/** Lifecycle status for a workspace (draft, active, closed, archived). */
public enum WorkspaceStatus {
    DRAFT,
    ACTIVE,
    CLOSED,
    ARCHIVED
}
